export type Role = "member" | "manager" | "admin";
export type Member = { id:string; auth_user_id?:string|null; department_id:string; first_name:string; last_name:string; email:string; badge_number?:string|null; rank?:string|null; station?:string|null; platoon?:string|null; role:Role; status:"active"|"inactive"|"leave"; shopify_company_contact_id?:string|null; shopify_company_location_id?:string|null; shopify_customer_id?:string|null };
export type Department = { id:string; name:string; code:string; fiscal_year:string; approval_required:boolean; approval_threshold:number; allow_personal_overage:boolean; shopify_shop_domain?:string|null; shopify_company_id?:string|null; shopify_company_name?:string|null; shopify_company_location_id?:string|null; shopify_sync_status?:string|null; shopify_last_sync_at?:string|null };
export type AllowanceAccount = { id:string; member_id:string; annual_amount:number; current_balance:number; reserved_amount:number; spent_amount:number; reset_date:string };
export type ProductVariant = { id:string; title:string; sku:string; price:number; available:boolean };
export type Product = { id:string; title:string; description:string; category:string; image_url?:string|null; price:number; allowance_eligible:boolean; approval_required:boolean; active:boolean; shopify_product_id:string; variants:ProductVariant[] };
export type ShopifyCollection = { id:string; shopify_collection_id:string; title:string; handle:string; image_url?:string|null; shopify_synced_at?:string|null };
export type PurchaseRequest = { id:string; request_number:string; member_id:string; member_name:string; status:string; total_amount:number; allowance_amount:number; personal_amount:number; submitted_at:string; shopify_order_name?:string|null; shopify_invoice_url?:string|null; tracking_number?:string|null };
export type LedgerEntry = { id:string; type:string; amount:number; balance_after:number; reason:string; created_at:string };
export type PortalContext = { demo:boolean; platformOwner:boolean; departments:Department[]; member:Member; department:Department; account:AllowanceAccount; members:Member[]; accounts:AllowanceAccount[]; products:Product[]; collections:ShopifyCollection[]; requests:PurchaseRequest[]; ledger:LedgerEntry[] };
